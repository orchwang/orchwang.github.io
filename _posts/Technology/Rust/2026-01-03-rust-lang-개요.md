---
layout: post
title: "Rust lang 개요"
date: 2026-01-03
categories: [Technology, Rust]
tags: [rust]
series: Rust-Essential
published: true
excerpt: "Rust 프로그래밍 언어에 대한 개요와 핵심 특징을 알아봅니다."
---

<figure class="post-figure post-figure--header">
<svg role="img" aria-label="Rust의 핵심 가치를 한 장에 담은 그림. 가운데 'Rust'를 중심으로 세 꼭짓점이 삼각형을 이룬다. 위쪽 꼭짓점은 메모리 안전성(소유권), 왼쪽 아래 꼭짓점은 성능(C/C++ 수준), 오른쪽 아래 꼭짓점은 병렬성(데이터 레이스 없음)을 가리킨다. 삼각형 아래에는 띠가 하나 있어, 다른 언어들이 메모리 안전을 위해 런타임 가비지 컬렉터를 두는 것과 달리 Rust는 컴파일 시점에 안전성을 검증해 GC가 필요 없음을 보여준다." viewBox="0 0 680 300" xmlns="http://www.w3.org/2000/svg">
  <title>Rust의 핵심 가치 — 메모리 안전성·성능·병렬성의 삼각, 그리고 GC 없는 컴파일타임 안전</title>

  <!-- ===== 삼각형: 세 가치 ===== -->
  <!-- 변(삼각형) -->
  <line x1="200" y1="58" x2="92" y2="186" stroke="var(--secondary-color)" stroke-width="2"/>
  <line x1="200" y1="58" x2="308" y2="186" stroke="var(--secondary-color)" stroke-width="2"/>
  <line x1="92" y1="186" x2="308" y2="186" stroke="var(--secondary-color)" stroke-width="2"/>

  <!-- 중심: Rust -->
  <circle cx="200" cy="142" r="34" fill="var(--bg-panel)" stroke="var(--gold)" stroke-width="2.5"/>
  <text x="200" y="138" text-anchor="middle" font-size="15" fill="currentColor" font-weight="700">Rust</text>
  <text x="200" y="153" text-anchor="middle" font-size="8" fill="currentColor" opacity="0.8">GC 없이</text>

  <!-- 꼭짓점 1: 메모리 안전성 (위) -->
  <rect x="148" y="26" width="104" height="40" rx="4" fill="var(--bg-light)" stroke="var(--accent-color)" stroke-width="2"/>
  <text x="200" y="44" text-anchor="middle" font-size="10" fill="currentColor" font-weight="700">메모리 안전성</text>
  <text x="200" y="57" text-anchor="middle" font-size="8" fill="currentColor" opacity="0.8">소유권 (Ownership)</text>

  <!-- 꼭짓점 2: 성능 (왼쪽 아래) -->
  <rect x="34" y="180" width="104" height="40" rx="4" fill="var(--bg-light)" stroke="currentColor" stroke-width="1.8"/>
  <text x="86" y="198" text-anchor="middle" font-size="10" fill="currentColor" font-weight="700">성능</text>
  <text x="86" y="211" text-anchor="middle" font-size="8" fill="currentColor" opacity="0.8">C/C++ 수준</text>

  <!-- 꼭짓점 3: 병렬성 (오른쪽 아래) -->
  <rect x="262" y="180" width="104" height="40" rx="4" fill="var(--bg-light)" stroke="currentColor" stroke-width="1.8"/>
  <text x="314" y="198" text-anchor="middle" font-size="10" fill="currentColor" font-weight="700">병렬성</text>
  <text x="314" y="211" text-anchor="middle" font-size="8" fill="currentColor" opacity="0.8">데이터 레이스 없음</text>

  <!-- divider -->
  <line x1="404" y1="40" x2="404" y2="252" stroke="currentColor" stroke-width="1" opacity="0.25"/>

  <!-- ===== 오른쪽: GC 있는 언어 vs Rust ===== -->
  <text x="544" y="34" text-anchor="middle" font-size="12" fill="currentColor" font-weight="700" opacity="0.75">안전성을 언제 보장하나</text>

  <!-- GC 언어 경로 -->
  <text x="544" y="64" text-anchor="middle" font-size="9.5" fill="currentColor" opacity="0.8" font-weight="700">다른 언어 (GC)</text>
  <rect x="436" y="74" width="78" height="34" rx="3" fill="var(--bg-light)" stroke="currentColor" stroke-width="1.8"/>
  <text x="475" y="92" text-anchor="middle" font-size="9" fill="currentColor" font-weight="700">컴파일</text>
  <text x="475" y="104" text-anchor="middle" font-size="7.5" fill="currentColor" opacity="0.8">검사 적음</text>
  <line x1="514" y1="91" x2="540" y2="91" stroke="var(--secondary-color)" stroke-width="2" marker-end="url(#rust-arrow)"/>
  <rect x="542" y="74" width="106" height="34" rx="3" fill="var(--bg-light)" stroke="var(--accent-color)" stroke-width="2"/>
  <text x="595" y="92" text-anchor="middle" font-size="9" fill="currentColor" font-weight="700">런타임 + GC</text>
  <text x="595" y="104" text-anchor="middle" font-size="7.5" fill="currentColor" opacity="0.8">실행 중 메모리 관리</text>

  <!-- Rust 경로 -->
  <text x="544" y="150" text-anchor="middle" font-size="9.5" fill="currentColor" opacity="0.8" font-weight="700">Rust</text>
  <rect x="436" y="160" width="148" height="34" rx="3" fill="var(--bg-panel)" stroke="var(--gold)" stroke-width="2.5"/>
  <text x="510" y="178" text-anchor="middle" font-size="9" fill="currentColor" font-weight="700">컴파일 (소유권 검증)</text>
  <text x="510" y="190" text-anchor="middle" font-size="7.5" fill="currentColor" opacity="0.8">여기서 안전성 확정</text>
  <line x1="584" y1="177" x2="610" y2="177" stroke="var(--secondary-color)" stroke-width="2" marker-end="url(#rust-arrow)"/>
  <rect x="612" y="160" width="40" height="34" rx="3" fill="var(--bg-light)" stroke="currentColor" stroke-width="1.8"/>
  <text x="632" y="174" text-anchor="middle" font-size="8" fill="currentColor" font-weight="700">런타임</text>
  <text x="632" y="186" text-anchor="middle" font-size="7" fill="currentColor" opacity="0.8">GC 없음</text>

  <text x="544" y="234" text-anchor="middle" font-size="9" fill="currentColor" opacity="0.8">안전 비용을 런타임에서 컴파일타임으로 옮긴다</text>

  <defs>
    <marker id="rust-arrow" markerWidth="8" markerHeight="8" refX="6" refY="4" orient="auto">
      <path d="M0,0 L8,4 L0,8 z" fill="var(--secondary-color)"/>
    </marker>
  </defs>
</svg>
<figcaption>Rust의 가치 제안 한 장 요약 — 왼쪽은 <strong>메모리 안전성·성능·병렬성</strong>의 삼각이고 그 중심에 "GC 없이"가 있습니다. 오른쪽은 그 비밀로, 다른 언어가 런타임 가비지 컬렉터로 메모리를 관리하는 대신 Rust는 <strong>컴파일 시점의 소유권 검증</strong>으로 안전을 확정해 런타임 GC를 없앱니다.</figcaption>
</figure>

## Rust란 무엇인가?

Rust는 성능과 안정성, 특히 메모리 안전성을 보장하면서도 가비지 컬렉터(Garbage Collector) 없이 동작하는 시스템 프로그래밍 언어입니다. Mozilla에서 처음 개발되었으며, 현재는 Rust Foundation이 관리하고 있습니다.

### 주요 특징

1.  **메모리 안전성 (Memory Safety)**: 컴파일 시점에 소유권(Ownership) 규칙을 통해 메모리 오류를 방지합니다.
2.  **성능 (Performance)**: C/C++에 버금가는 실행 속도를 자랑합니다.
3.  **병렬성 (Concurrency)**: 데이터 레이스(Data Race) 없는 안전한 병렬 프로그래밍을 지원합니다.

Rust의 위치를 가장 또렷이 보여주는 것은 메모리를 다루는 방식을 다른 진영과 나란히 놓고 비교할 때입니다.

<figure class="post-figure">
<svg role="img" aria-label="세 가지 메모리 관리 방식을 세 칸으로 비교한 그림. 왼쪽 칸은 C/C++로, 개발자가 직접 할당과 해제를 책임지며 안전은 보장되지 않는다. 가운데 칸은 Java나 Go 같은 GC 언어로, 런타임 가비지 컬렉터가 자동으로 메모리를 회수하지만 실행 중 멈춤과 오버헤드가 생긴다. 오른쪽 칸은 Rust로, 컴파일러가 소유권 규칙으로 해제 시점을 결정해 GC도 수동 해제도 없이 안전을 보장한다." viewBox="0 0 660 280" xmlns="http://www.w3.org/2000/svg">
  <title>메모리 관리 방식 비교 — C/C++(수동) · GC 언어(자동·런타임) · Rust(소유권·컴파일타임)</title>

  <!-- ===== 칸 1: C/C++ (수동) ===== -->
  <rect x="16" y="40" width="196" height="206" rx="5" fill="var(--bg-light)" stroke="currentColor" stroke-width="1.8"/>
  <text x="114" y="64" text-anchor="middle" font-size="13" fill="currentColor" font-weight="700">C / C++</text>
  <text x="114" y="80" text-anchor="middle" font-size="9" fill="currentColor" opacity="0.8">수동 관리</text>
  <line x1="36" y1="92" x2="192" y2="92" stroke="currentColor" stroke-width="1" opacity="0.25"/>
  <text x="114" y="118" text-anchor="middle" font-size="10" fill="currentColor" font-weight="700">개발자가 직접</text>
  <text x="114" y="135" text-anchor="middle" font-size="9" fill="currentColor" opacity="0.85">malloc / free</text>
  <text x="36" y="168" font-size="9.5" fill="currentColor" opacity="0.85">+ 최고의 성능·제어</text>
  <rect x="32" y="190" width="164" height="44" rx="3" fill="var(--bg-panel)" stroke="var(--accent-color)" stroke-width="2"/>
  <text x="114" y="208" text-anchor="middle" font-size="9" fill="currentColor" font-weight="700">안전은 보장 안 됨</text>
  <text x="114" y="223" text-anchor="middle" font-size="8" fill="currentColor" opacity="0.8">누수·이중 해제·dangling</text>

  <!-- ===== 칸 2: GC 언어 (자동·런타임) ===== -->
  <rect x="232" y="40" width="196" height="206" rx="5" fill="var(--bg-light)" stroke="currentColor" stroke-width="1.8"/>
  <text x="330" y="64" text-anchor="middle" font-size="13" fill="currentColor" font-weight="700">Java · Go · Python</text>
  <text x="330" y="80" text-anchor="middle" font-size="9" fill="currentColor" opacity="0.8">자동 관리 (런타임 GC)</text>
  <line x1="252" y1="92" x2="408" y2="92" stroke="currentColor" stroke-width="1" opacity="0.25"/>
  <text x="330" y="118" text-anchor="middle" font-size="10" fill="currentColor" font-weight="700">가비지 컬렉터가</text>
  <text x="330" y="135" text-anchor="middle" font-size="9" fill="currentColor" opacity="0.85">실행 중 자동 회수</text>
  <text x="252" y="168" font-size="9.5" fill="currentColor" opacity="0.85">+ 안전·개발 편의</text>
  <rect x="248" y="190" width="164" height="44" rx="3" fill="var(--bg-panel)" stroke="var(--accent-color)" stroke-width="2"/>
  <text x="330" y="208" text-anchor="middle" font-size="9" fill="currentColor" font-weight="700">런타임 비용</text>
  <text x="330" y="223" text-anchor="middle" font-size="8" fill="currentColor" opacity="0.8">멈춤(pause)·오버헤드</text>

  <!-- ===== 칸 3: Rust (소유권·컴파일타임) ===== -->
  <rect x="448" y="40" width="196" height="206" rx="5" fill="var(--bg-panel)" stroke="var(--gold)" stroke-width="2.5"/>
  <text x="546" y="64" text-anchor="middle" font-size="13" fill="currentColor" font-weight="700">Rust</text>
  <text x="546" y="80" text-anchor="middle" font-size="9" fill="currentColor" opacity="0.8">소유권 (컴파일타임)</text>
  <line x1="468" y1="92" x2="624" y2="92" stroke="currentColor" stroke-width="1" opacity="0.25"/>
  <text x="546" y="118" text-anchor="middle" font-size="10" fill="currentColor" font-weight="700">컴파일러가 소유권으로</text>
  <text x="546" y="135" text-anchor="middle" font-size="9" fill="currentColor" opacity="0.85">해제 시점 자동 결정</text>
  <text x="468" y="168" font-size="9.5" fill="currentColor" opacity="0.85">+ 성능 · 안전 둘 다</text>
  <rect x="464" y="190" width="164" height="44" rx="3" fill="var(--bg-light)" stroke="var(--secondary-color)" stroke-width="2"/>
  <text x="546" y="208" text-anchor="middle" font-size="9" fill="currentColor" font-weight="700">GC도 수동 해제도 없음</text>
  <text x="546" y="223" text-anchor="middle" font-size="8" fill="currentColor" opacity="0.8">런타임 비용 없이 안전</text>
</svg>
<figcaption>같은 "메모리 관리"라도 푸는 방식이 다릅니다. <strong>C/C++</strong>는 개발자가 직접 책임지지만 안전이 보장되지 않고, <strong>GC 언어</strong>는 런타임 가비지 컬렉터로 안전하되 멈춤·오버헤드를 치릅니다. <strong>Rust</strong>는 컴파일 시점의 소유권 검증으로 해제 시점을 정해, 두 진영의 약점 없이 성능과 안전을 함께 가져갑니다.</figcaption>
</figure>

## Rust 학습 로드맵

Rust는 학습 곡선(Learning Curve)이 다소 가파른 언어로 알려져 있습니다. 체계적인 학습 계획이 중요합니다.

### 1단계: 기초 문법과 환경 설정

- **Rust 설치**: `rustup`을 이용한 툴체인 설치.
- **Hello World**: `cargo` 빌드 시스템 이해.
- **기본 문법**: 변수, 가변성(`mut`), 데이터 타입, 함수, 흐름 제어(`if`, `loop`, `while`, `for`).
- **기본 자료구조**: 튜플(Tuple), 배열(Array).

### 2단계: 소유권(Ownership) 시스템 - 핵심

Rust의 가장 독창적이고 중요한 개념입니다. 이 부분을 확실히 이해해야 합니다.

- **소유권 규칙**: 이동(Move)과 복사(Copy).
- **빌림(Borrowing)**: 참조(`&`)와 가변 참조(`&mut`).
- **슬라이스(Slice)**: 컬렉션의 연속된 요소 참조.

### 3단계: 구조적 데이터와 패턴 매칭

- **Structs**: 데이터 구조 정의.
- **Enums**: 열거형과 `Option<T>` 타입.
- **Pattern Matching**: `match` 흐름 제어 연산자와 `if let`.

### 4단계: 에러 처리와 컬렉션

- **Collections**: `Vector`, `String`, `HashMap`.
- **Error Handling**: `Result<T, E>`와 `panic!`, `?` 연산자.

### 5단계: 제네릭, 트레이트, 라이프타임

추상화와 코드 재사용을 위한 고급 기능입니다.

- **Generics**: 데이터 타입의 추상화.
- **Traits**: 공통 동작 정의 (타 언어의 인터페이스와 유사).
- **Lifetimes**: 참조자의 유효 범위 검증.

### 6단계: 고급 기능 및 프로젝트

- **Smart Pointers**: `Box<T>`, `Rc<T>`, `RefCell<T>`.
- **Concurrency**: 스레드, 메시지 패싱, 공유 상태.
- **Automated Tests**: 단위 테스트 및 통합 테스트 작성.
- **I/O Project**: 커맨드 라인 도구 만들기 (예: `grep` 구현).

### 7단계: 디버깅 및 프로파일링

- **Debugging**: `dbg!` 매크로 활용, `gdb`/`lldb` 디버거 연동.
- **Profiling**: `criterion` 크레이트를 이용한 벤치마킹, Flamegraph 등 성능 분석 도구.

### 8단계: TDD with Rust

- **Test Driven Development**: Red-Green-Refactor 사이클 이해 및 적용.
- **Mocking**: `mockall` 등의 크레이트를 활용한 테스트 더블 작성.
- **Integration Testing**: 라이브러리 구조와 통합 테스트 패턴.

## 추천 학습 자료

1.  **The Rust Programming Language (The Book)**: 공식 문서이자 최고의 입문서입니다.
2.  **Rustlings**: 작은 코드를 수정하며 배우는 실습형 튜토리얼입니다.
3.  **Rust by Example**: 예제 코드를 통해 배우는 방식입니다.

## 마무리

Rust는 처음에는 컴파일러와의 싸움이 될 수 있지만, 그 과정을 통해 더 안전하고 견고한 코드를 작성하는 습관을 기를 수 있습니다. 이 시리즈를 통해 Rust의 핵심 개념들을 하나씩 정복해 나갈 예정입니다.
